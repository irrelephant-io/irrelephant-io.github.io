import React from 'react';
import { Content, Header } from '../components';

const Main: React.FC = () => {
  return (
    <div>
      <Header />
      <Content />
    </div>
  );
};

export default Main;
